# HiAstra Markdown Preview

> A calm dark theme with **vibrant** accents and `inline code`.

Compare *italic*, **bold**, and ***bold italic*** with plain text and `inline code`.

## Links and lists

- Read the [HiAstra documentation](https://example.test/hiastra).
- Inspect `#55D6E6`, `#B89CFF`, and `#F08ACB`.
- [ ] Check headings, emphasis, and task markers.
- [x] Check links and inline code.

### Token map

| Category | Color | Meaning |
| --- | --- | --- |
| Function | `#6FA8FF` | Blue |
| Type | `#B89CFF` | Purple |
| String | `#72E0B2` | Mint |
| Inline code | `#F08ACB` | Magenta |

```javascript
const themeName = "HiAstra";
function preview(value) {
	return `${themeName}: ${value}`;
}
```

<!-- TODO: inspect headings, links, code spans, tables, quotes, and fences. -->
